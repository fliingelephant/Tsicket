name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: build vue
        run: |
          npm --version
      - name: test rust
        run: |
          cargo check --verbose --all


